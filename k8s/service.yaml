---

apiVersion: v1
kind: Service
metadata:
  name: my-portfolio-deployment
spec:
  type: LoadBalancer
  selector:
    app: my-portfolio
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80